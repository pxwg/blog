// Minimal test for HTML export without dependencies
#set page(paper: "a4")
#set text(font: "Linux Libertine", size: 12pt)

= Simple HTML Export Test

This is a basic test document to check HTML export functionality.

== Text Content
Regular text should render without issues.

== Simple Math
Basic inline math: $x + y = z$

Basic display math:
$ f(x) = x^2 + 2x + 1 $

== Without Font Dependencies
This test uses default fonts to avoid font-related issues.